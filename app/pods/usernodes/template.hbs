<!-- app/pods/usernodes/template.hbs -->
User: {{model.fullName}}<br>
Projects:
<ol>
    {{#each allRelated as |node|}}
        <li>{{node}}, {{node.title}}</li>
    {{/each}}
</ol>

<button {{action 'getMore'}}>Load more</button>
